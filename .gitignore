# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*

# Maven
target/
.idea/
*.iml

# The OAuth client secret. auth.yaml.example beside it is committed and shows the
# shape; the real file never is. If this line is ever removed and a secret pushed,
# revoke the client in the Google Cloud console — rewriting history is not enough,
# the value is public the moment it lands on a remote.
data/config/auth.yaml

# Local JSON store
data/store/

# Timestamped store snapshots
data/backups/

# Archived pre-v2 store (the SailSys-era data, kept for the future importer).
# Not committed: the repo is public and this is real club race data. Promote a
# sanitised race or two to src/test/resources if you want importer fixtures.
data/archive/
